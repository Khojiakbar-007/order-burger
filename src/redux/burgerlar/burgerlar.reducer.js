const INITIAL_STATE = {
  buyurtmaQilinganBurgerlar: [],
};

const burgerReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default burgerReducer;
